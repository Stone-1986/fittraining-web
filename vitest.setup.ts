// Matchers de DOM (`toBeInTheDocument`, `toHaveAccessibleName`...) para los
// tests de componente que escribira el QA. Se registra aqui una vez y no en
// cada archivo de test.
import '@testing-library/jest-dom/vitest';

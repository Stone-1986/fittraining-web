import { SiteHeader } from 'fittraining';

export const Inicio = () => (
  <div className="bg-background">
    <SiteHeader active="inicio" />
  </div>
);

export const Preguntas = () => (
  <div className="bg-background">
    <SiteHeader active="preguntas" />
  </div>
);

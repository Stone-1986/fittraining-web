import { ClosingCta } from 'fittraining';

// El unico boton primario de la portada. La foto de `/fotos/` no existe en
// Claude Design: bajo el velo se ve el rayado del hueco.
export const Default = () => (
  <div className="bg-background">
    <ClosingCta />
  </div>
);
